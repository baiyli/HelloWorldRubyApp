version: 1.0
runtime: ruby31
build:
  commands:
    build:
      - echo $CODEBUILD_BUILD_IMAGE
      - bundle install
run:
  runtime-version: 3.1.2
  command: bundle exec rackup --host 0.0.0.0 -p 4567
  network:
    port: 4567
    env: APP_PORT
